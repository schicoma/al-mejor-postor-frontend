import { CanActivateViaAuth } from './can-activate-via-auth';

describe('CanActivateViaAuth', () => {
  it('should create an instance', () => {
    expect(new CanActivateViaAuth()).toBeTruthy();
  });
});
